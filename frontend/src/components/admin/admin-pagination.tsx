import type { Pagination } from "@/api/adminApi";
export function AdminPagination({pagination,page,pageSize,onPage,onPageSize}:{pagination?:Pagination;page:number;pageSize:number;onPage:(page:number)=>void;onPageSize:(size:number)=>void}){if(!pagination)return null;return <div className="flex items-center justify-between border-t p-4 text-xs"><select value={pageSize} onChange={e=>onPageSize(Number(e.target.value))} className="rounded-lg border px-2 py-2">{[10,25,50].map(n=><option key={n}>{n} per page</option>)}</select><span className="text-bank-muted">Page {pagination.page} of {Math.max(1,pagination.totalPages)} · {pagination.total} records</span><div className="flex gap-2"><button disabled={page<=1} onClick={()=>onPage(page-1)} className="rounded-lg border px-3 py-2 disabled:opacity-40">Previous</button><button disabled={page>=pagination.totalPages} onClick={()=>onPage(page+1)} className="rounded-lg border px-3 py-2 disabled:opacity-40">Next</button></div></div>}
